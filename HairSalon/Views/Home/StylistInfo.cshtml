<div class="container">
        <h2>Information about Stylist</h2>
        <br>
        @if (Model != null)
        {
          <h4>Stylist Name: @Model.GetName()</h4>
          <br>
          <h5>List of clients</h5>
          <br>

        }
      <button type="button" class="btn"><a href="/">Home</a></button>
</div>
